@inject Microsoft.Extensions.Localization.IStringLocalizer<AddEditMemberModal> _localizer

<EditForm Model="@AddEditMemberModel" OnValidSubmit="SaveAsync">
    <FluentValidationValidator @ref="_fluentValidationValidator" />
    <MudDialog>
        <TitleContent>
            @{
                if (AddEditMemberModel.Id == 0)
                {
                    <MudText Typo="Typo.h6">
                        <MudIcon Icon="@Icons.Material.Filled.Add" Class="mr-3 mb-n1" />
                        @_localizer["Add Product"]
                    </MudText>
                }
                else
                {
                    <MudText Typo="Typo.h6">
                        <MudIcon Icon="@Icons.Material.Filled.Update" Class="mr-3 mb-n1" />
                        @_localizer["Update Product"]
                    </MudText>
                }
            }
        </TitleContent>
        <DialogContent>
            <MudGrid>
                @if (AddEditMemberModel.Id != 0)
                {
                    <MudItem xs="12" md="6">
                        <MudTextField Disabled For="@(() => AddEditMemberModel.Id)" @bind-Value="AddEditMemberModel.Id" Label="@_localizer["Id"]" />
                    </MudItem>
                }
                <MudItem xs="12" md="6">
                    <MudTextField For="@(() => AddEditMemberModel.CustomerId)" @bind-Value="AddEditMemberModel.CustomerId" Label="@_localizer["CustomerId"]" />
                </MudItem>
                <MudItem xs="12" md="6">
                    <MudTextField Disabled For="@(() => AddEditMemberModel.CreatedAt)" @bind-Value="AddEditMemberModel.CreatedAt" Label="@_localizer["Created"]" />
                </MudItem>
               
                <MudItem xs="12" md="6">
                    <MudTextField Disabled For="@(() => AddEditMemberModel.UpdatedAt)" @bind-Value="AddEditMemberModel.UpdatedAt" Label="@_localizer["Updated"]" />
                </MudItem>
            </MudGrid>
        </DialogContent>
        <DialogActions>
            <MudButton DisableElevation Variant="Variant.Filled" OnClick="Cancel">@_localizer["Cancel"]</MudButton>
            @if (AddEditMemberModel.Id != 0)
            {
                <MudButton DisableElevation Variant="Variant.Filled" ButtonType="ButtonType.Submit" Disabled="@(!Validated)" Color="Color.Secondary">@_localizer["Update"]</MudButton>
            }
            else
            {
                <MudButton DisableElevation Variant="Variant.Filled" ButtonType="ButtonType.Submit" Disabled="@(!Validated)" Color="Color.Success">@_localizer["Save"]</MudButton>
            }
        </DialogActions>
    </MudDialog>
</EditForm>